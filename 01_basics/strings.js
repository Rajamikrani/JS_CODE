const name = "Raja Mikrani"
const goal = "Good at Programming"
const newStr = new String("Hello World")
goal.toUpperCase() // if we use like it and print the goal then its not works.
const goalUpper = goal.toUpperCase()

console.log(newStr.toUpperCase())
console.log(goalUpper.bold()) // if we use bold() method then its return in <b>value</b>

